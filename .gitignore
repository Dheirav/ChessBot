# Build artifacts
*.o
*.d
chessbot

# Test binaries (built by `make tests`)
tests/perft
tests/match
tests/gamestate

# Move lookup tables are cached next to whichever binary generated them
lookup_data/
tests/src/

# Profiling and logs
gmon.out
evaluation_log_*.txt

# Evaluation dumps produced by GameManager::logEvaluation()
evaluation_log_*.txt

# Match gate logs (tee output, often still being written)
gate-*.log
